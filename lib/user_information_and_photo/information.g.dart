// GENERATED CODE - DO NOT MODIFY BY HAND

part of 'information.dart';

// **************************************************************************
// FunctionalWidgetGenerator
// **************************************************************************

class Username extends StatelessWidget {
  const Username({
    Key? key,
    required this.text,
  }) : super(key: key);

  final String text;

  @override
  Widget build(BuildContext _context) => username(
        _context,
        text: text,
      );
}

class UserLocation extends StatelessWidget {
  const UserLocation({
    Key? key,
    required this.location,
  }) : super(key: key);

  final String location;

  @override
  Widget build(BuildContext _context) => userLocation(
        _context,
        location: location,
      );
}

class UserPhoto extends StatelessWidget {
  const UserPhoto({
    Key? key,
    required this.image,
  }) : super(key: key);

  final String image;

  @override
  Widget build(BuildContext _context) => userPhoto(
        _context,
        image: image,
      );
}
